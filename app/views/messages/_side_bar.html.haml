.side
  .side__header
    .side__header--text
      .side__header--text--username
        = current_user.name
      .side__header--text--icon
        = link_to "#" do
          =icon('far', 'edit')
        %span<>
        =link_to edit_user_path(current_user) do
          =icon('fas', 'cog')
  .side__groups
    .group
      .group--name
        aaa
      .group--last--message
        bbb
